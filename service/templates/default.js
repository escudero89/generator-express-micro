'use strict';

var commons = require('../commons.js');

var proto
  , repo = commons.repository
  ;

module.exports = function <%= name[0].toUpperCase() + name.slice(1) %>Service() {
  // service logic goes here.
};

proto = module.exports.prototype;
